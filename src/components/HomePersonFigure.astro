---
import DoodleImage from "./DoodleImage.astro";

type Props = {
  src: string;
  reducedMotionSrc: string;
};

const { src, reducedMotionSrc } = Astro.props;
---

<DoodleImage
  src={src}
  reducedMotionSrc={reducedMotionSrc}
  alt="Hand-drawn animation of Sophie spinning"
  variant="avatar"
  animation="none"
  fetchPriority="high"
/>
